name: poc_dimensional_modeling
version: '1.0.0'
config-version: 2

profile: poc_dimensional_modeling

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  poc_dimensional_modeling:
    staging:
      +materialized: table
    intermediate:
      +materialized: table
    marts:
      +materialized: table
