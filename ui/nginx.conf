server {
    listen 3000;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /bq/ {
        proxy_pass http://bigquery-emulator:9050/bigquery/v2/;
        proxy_set_header Host $host;
    }
}
